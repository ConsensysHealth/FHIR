image: gitpod/workspace-full

ports: 
 - port: 9444

tasks:
 - init: mvn clean install -f fhir-examples -DskipTests
 - init: mvn clean install -f fhir-parent -DskipTests
   command: docker build fhir-install
 
